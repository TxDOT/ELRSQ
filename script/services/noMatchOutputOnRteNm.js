/**
 *
 * @param {*} unfilteredArr
 * @returns
 */
function noMatchOutputOnRteNm(unfilteredArr) {
  let results0 = unfilteredArr[0];
  let output0 = results0[0];
  console.log(output0);
  let nomatch = { ...output0 };
  return (nomatch);
}
